import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { IonDividerComponent } from '../projects/ion/src/lib/divider/divider.component';

<Meta title="Ion/Design Tokens/Dividers" component={IonDividerComponent} />

export const Template = (args) => <IonDividerComponent {...args} />;

# Divider

<Canvas>
  <Story name="type: default divider">{Template.bind({})}</Story>
</Canvas>

No Ion, a orientação padrão do divisor é `horizontal`. Mas fornecemos 2 tipos de orientação:

- `horizontal`. Indica que o divisor está na orientação horizontal.

<Canvas>
  <Story name="type: div with horizontal divider">
    {{
      template: `<div style="width: 150px"><ion-divider direction="horizontal"> </ion-divider></div>`,
    }}
  </Story>
</Canvas>

O tamanho do divisor `horizontal` será definido pelo elemento HTML em que o mesmo está contido.

<Canvas>
  <Story
    name="type: horizontal divider"
    args={{
      direction: 'horizontal',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

- `vertical`. Indica que o divisor está na orientação vertical.

<Canvas>
  <Story
    name="type: vertical divider"
    args={{
      direction: 'vertical',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

O tamanho do divisor `vertical` será definido pelo elemento HTML em que o mesmo está contido.

<Canvas>
  <Story name="type: div with vertical divider">
    {{
      template: `<div style="height: 150px"><ion-divider direction="vertical"> </ion-divider></div>`,
    }}
  </Story>
</Canvas>

No Ion, o divisor possui 3 tipos.

- `solid`. Divisor sólido.

<Canvas>
  <Story
    name="type: solid divider"
    args={{
      type: 'solid',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

- `dashed`. Divisor tracejado.

<Canvas>
  <Story
    name="type: dashed divider"
    args={{
      type: 'dashed',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

- `text`. Divisor com uma `label` em seu centro.

<Canvas>
  <Story
    name="type: text divider"
    args={{
      type: 'text',
      label: 'Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

Nota-se: Divisores do tipo `text` só estarão disponíveis na `direction: horizontal`,
porém não será necessário passar tipo `direction`, pois `horizontal` será o tipo
padrão desse divisor.

- Todas as combinações possíveis do ion-divider:

<Canvas>
  <Story name="type: default" args={{}}>
    {Template.bind({})}
  </Story>
  <Story
    name="type: dashed"
    args={{
      type: 'dashed',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="type: vertical"
    args={{
      direction: 'vertical',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="type: vertical dashed"
    args={{
      direction: 'vertical',
      type: 'dashed',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="type: text"
    args={{
      type: 'text',
      label: 'Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
