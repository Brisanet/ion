import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { AlertComponent } from '../projects/ion/src/lib/alert/alert.component.ts';
import { IonIconComponent } from '../projects/ion/src/lib/icon/icon.component.ts';
import { CommonModule } from '@angular/common';

<Meta title="Ion/Alert" component={AlertComponent} />

export const Template = (args) => <AlertComponent {...args} />;

# Alert

No Ion o alerta pode receber 5 parâmetros, sendo eles:

- `message`. Indicando a mensagem que será mostrada no alerta.
- `type`. Indicando o tipo do alerta.
- `closable`. Indicando se o alerta pode ou não ser fechado.
- `width`. Indicando a largura do alerta.
- `height`. Indicando a altura do alerta.

Dentre os parâmetros o `message` é o unico obrigatório, os outros possuem valores padrões.
O alerta pode ser chamado da seguinte forma:

- `<ion-alert message="Example"></ion-alert>`

## Message

O parâmetro `message` define qual será a mensagem exibida no alerta

<Canvas>
  <Story
    name="type: message"
    args={{
      message: 'Trying a new message',
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Type

O ion-alert possui um total de 4 tipos: `success`, `alert`, `info`, `danger`

- Por padrão, caso não seja informado o tipo pelo usuario, o alerta será do tipo `success`

#### Sucesso(success)

<Canvas>
  <Story
    name="type: success"
    args={{
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Aviso(alert)

<Canvas>
  <Story
    name="type: alert"
    args={{
      type: 'alert',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Informação(info)

<Canvas>
  <Story
    name="type: info"
    args={{
      type: 'info',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Erro(danger)

<Canvas>
  <Story
    name="type: danger"
    args={{
      type: 'danger',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Closable

A partir do paramêtro `closable` pode ser definido se o usuárario poderá fechar, ou não, o alerta.

- `closable` aceita os valores `true` ou `false`. E o mesmo por padrão traz `false`

<Canvas>
  <Story
    name="type: closable"
    args={{
      message: 'Example of alert message',
      closable: true,
    }}
    decorators={[
      moduleMetadata({
        declarations: [IonIconComponent],
        imports: [CommonModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>
