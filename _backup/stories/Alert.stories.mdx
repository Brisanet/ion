import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { IonAlertComponent } from '../projects/ion/src/lib/alert/alert.component.ts';
import { IonSharedModule } from '../projects/ion/src/public-api';
import { CommonModule } from '@angular/common';

<Meta title="Ion/Feedback/Alert" component={IonAlertComponent} />

export const Template = (args) => <IonAlertComponent {...args} />;

# Alert

No Ion o alerta pode receber 5 parâmetros, sendo eles:

- `message`. Indicando o conteúdo que será mostrada no alerta, podendo ser uma string ou um TemplateRef.
- `type`. Indicando o tipo do alerta.
- `closable`. Indicando se o alerta pode ou não ser fechado.
- `width`. Indicando a largura do alerta.
- `height`. Indicando a altura do alerta.

Dentre os parâmetros o `message` é o unico obrigatório, os outros possuem valores padrões.
O alerta pode ser chamado das seguintes formas:

- `<ion-alert message="Example"></ion-alert>`
- `<ion-alert message="BodyTemplate"></ion-alert> <ng-template #BodyTemplate>Custom <span>Alert</span> Message </ng-template>`

## Message

O parâmetro `message` define qual será a mensagem exibida no alerta

<Canvas>
  <Story
    name="type: message"
    args={{
      message: 'Trying a new message',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Type

O ion-alert possui um total de 4 tipos: `success`, `warning`, `info`, `negative`

- Por padrão, caso não seja informado o tipo pelo usuario, o alerta será do tipo `success`

#### Sucesso(success)

<Canvas>
  <Story
    name="type: success"
    args={{
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Aviso(warning)

<Canvas>
  <Story
    name="type: warning"
    args={{
      type: 'warning',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Informação(info)

<Canvas>
  <Story
    name="type: info"
    args={{
      type: 'info',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#### Erro(negative)

<Canvas>
  <Story
    name="type: negative"
    args={{
      type: 'negative',
      message: 'Example of alert message',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Closable

A partir do paramêtro `closable` pode ser definido se o usuário poderá fechar, ou não, o alerta.

- `closable` aceita os valores `true` ou `false`. E o mesmo por padrão traz `false`

<Canvas>
  <Story
    name="type: closable"
    args={{
      message: 'Example of alert message',
      closable: true,
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Without Background

A partir do paramêtro `hideBackground` pode ser definido se o usuário quer mostrar o alerta com ou sem background.

- `hideBackground` aceita os valores `true` ou `false`. E o mesmo por padrão traz `false`

<Canvas>
  <Story
    name="type: without background"
    args={{
      message: 'Example of alert message',
      hideBackground: true,
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="type: with description"
    args={{
      type: 'info',
      message: 'Example of alert message',
      description: 'Example of description.',
    }}
    decorators={[
      moduleMetadata({
        imports: [CommonModule, IonSharedModule],
      }),
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>
